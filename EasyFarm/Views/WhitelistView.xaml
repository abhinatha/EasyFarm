<UserControl x:Class="EasyFarm.Views.WhitelistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="271"
             d:DesignWidth="442">
    <UserControl.Resources>
        <Style TargetType="Button"
               BasedOn="{StaticResource MetroButton}">
            <Setter Property="Width"
                    Value="80" />
            <Setter Property="Height"
                    Value="23" />
            <Setter Property="Margin"
                    Value="5" />
        </Style>
    </UserControl.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="5"
                   TextWrapping="Wrap"
                   Text="Only these characters may issue chat commands. An empty list allows everyone. Your own character is always allowed." />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Margin="5"
                     VerticalContentAlignment="Center"
                     ToolTip="Character name to allow"
                     Text="{Binding EntryText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1"
                    Content="Add"
                    Command="{Binding AddCommand}" />
            <Button Grid.Column="2"
                    Content="Remove"
                    Command="{Binding RemoveCommand}" />
        </Grid>

        <ListBox Grid.Row="2"
                 Margin="5"
                 ItemsSource="{Binding Names}"
                 SelectedItem="{Binding SelectedName}" />
    </Grid>
</UserControl>
